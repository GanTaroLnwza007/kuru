# Backend runtime dependencies for graders who expect requirements.txt.
# Preferred setup is still:
#   uv sync
#   uv pip install -e ../pipeline

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
supabase>=2.10.0
python-dotenv>=1.0.0
